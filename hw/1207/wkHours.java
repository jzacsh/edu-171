/**
 * Class for managing a given week of employee hours.
 * - course:     COMP-171-801RL
 * - assignment: pg 207; que. #6.23; due: 12/07
 *
 * @author Jonathan Zacsh <jzacsh@gmail.com>
 * @return string a table displaying the total hours worked by each employee
 * for a given week.
 */
public class wkHours {
  public static void main() {
   // Pass an array of total hours, in its original employee-order to
   // sortTotals() to receive a new array of each employees' newly sorted position
  }

  /**
   * Re-sort an array of numbers, descending.
   *
   *  @param  array  Array of total hours for each employee.
   *  @return array  New sort order for each employee's number.
   *    Original numeric position of each employee, stored in new positions
   *    throughout array, representing the sort order.
   */
  protected double[] sortTotals(double[] total) {
    //
  }

  /**
   * Total the values in a given array.
   */
  protected double wkTotal() {
  }

  protected double[] getTotals() {
  }
}
